# dexno

### To-do
- Reverse game-mode 🛠️ *(hide the pokemon sprite in number to name mode)*
- Language options ✅
- Put all Pokémon data into a local json (to optimize fetch time) ✅
- Dark mode ✅
- Add more text so everything is clearer (with translations :/)